import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pedro.fishingcompanion',
  appName: 'Pedro Fishing Companion',
  webDir: 'dist'
};

export default config;
